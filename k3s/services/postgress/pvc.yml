apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-local-data-pvc7
  namespace: postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 30Gi
